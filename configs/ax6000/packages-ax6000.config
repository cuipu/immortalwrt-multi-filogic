# packages-ax6000.config
# Redmi AX6000 专用插件包配置（diffconfig 风格，已完整合并）
# 已去除与 ax6000.config 中重复的包（如 kmod、ppp 等）
# 包含之前所有推荐插件（包括你提供的原始版本中未出现的：iptables、ipset、tc、zram-swap 等）
# 排序优化：基础优先 → 功能分组 → 可选代理最后
# 目标：功能全面、界面友好、性能优秀、固件大小适中

# ====================== 核心系统与 LuCI 基础（必须，最优先） ======================
CONFIG_PACKAGE_luci=y                         # LuCI 主框架（Web 管理界面）
CONFIG_PACKAGE_luci-base=y                     # LuCI 基础模块
CONFIG_PACKAGE_luci-mod-admin-full=y          # 完整后台管理功能
CONFIG_PACKAGE_luci-app-firewall=y            # 防火墙管理
CONFIG_PACKAGE_luci-app-opkg=y                # 软件包管理（opkg）

# ====================== 主题（美观度提升，推荐） ======================
CONFIG_PACKAGE_luci-theme-bootstrap=y         # 默认 Bootstrap 主题（轻量、稳定）
CONFIG_PACKAGE_luci-theme-argon=y             # Argon 现代主题（更美观、响应式，强烈推荐）

# ====================== 网络基础功能（常用） ======================
CONFIG_PACKAGE_luci-app-upnp=y                # UPnP 自动端口映射（游戏、BT 下载必备）
CONFIG_PACKAGE_luci-app-ddns=y                # 动态域名解析（外网访问）
CONFIG_PACKAGE_luci-app-wireguard=y           # WireGuard VPN（轻量高效）
CONFIG_PACKAGE_luci-app-unbound=y             # Unbound 递归 DNS 解析（隐私保护、可防污染）

# ====================== 性能与 QoS（MTK 平台关键） ======================
CONFIG_PACKAGE_luci-app-turboacc-mtk=y        # TurboACC（MTK 专用硬件加速配置界面，包括 HNAT、Flow Offload）
CONFIG_PACKAGE_luci-app-eqos-mtk=y            # MTK 优化版限速（EQoS，比普通版更精准）
CONFIG_PACKAGE_luci-app-sqm=y                 # SQM 智能队列管理（防缓冲膨胀，吃鸡/视频更流畅）

# ====================== 广告拦截与安全 ======================
CONFIG_PACKAGE_luci-app-adblock=y              # 广告拦截（简单高效）
CONFIG_PACKAGE_adblock=y                      # adblock 核心包

# ====================== 文件共享与终端 ======================
CONFIG_PACKAGE_luci-app-samba4=y              # Samba4 文件共享（局域网访问路由器存储）
CONFIG_PACKAGE_luci-app-ttyd=y                # Web 终端（浏览器直接 SSH 操作）

# ====================== 系统工具与优化（实用，必加） ======================
CONFIG_PACKAGE_luci-app-wol=y                 # Wake-on-LAN（局域网唤醒电脑）
CONFIG_PACKAGE_luci-app-statistics=y         # 流量监控与统计（集客插件）
CONFIG_PACKAGE_openssh-sftp-server=y         # SFTP 服务（安全文件传输）
CONFIG_PACKAGE_ethtool=y                     # 以太网工具（查看网口状态）
CONFIG_PACKAGE_iptables=y                    # iptables（基础防火墙工具）
CONFIG_PACKAGE_ipset=y                       # ipset（高效 IP 集合管理，常用于防火墙规则）
CONFIG_PACKAGE_tc=y                          # tc 流量控制（高级 QoS 基础）
CONFIG_PACKAGE_zram-swap=y                   # ZRAM 压缩交换（提升低内存设备流畅度，AX6000 1GB RAM 推荐开启）

# ====================== 可选代理插件（根据需求开启，若不需要可注释） ======================
# CONFIG_PACKAGE_luci-app-clash=y             # Clash 代理（需额外 feeds，如 helloworld）
# CONFIG_PACKAGE_luci-app-passwall=y          # Passwall（功能强大，包含多种代理协议，需额外 feeds）

# ====================== 节省空间（可选关闭不常用功能） ======================
# CONFIG_PACKAGE_ipv6helper=n                 # 如不需要 IPv6 助手可关闭
# CONFIG_PACKAGE_luci-app-ssr-plus=n          # 如不使用 SSR+ 可关闭