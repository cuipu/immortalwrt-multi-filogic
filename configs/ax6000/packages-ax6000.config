# This file specifies necessary and recommended packages for the Redmi AX6000 build.
# It ensures PPPoE dial-up support and adds useful LuCI apps/plugins for management.
# These are appended to the base config during build.
# Necessary: PPPoE for internet dial-up.
# Recommended: Web UI (LuCI), themes, adblock, UPnP, file sharing, QoS, etc.
# Version: Compatible with ImmortalWrt 24.10+; assumes packages are available in feeds.
# Note: This is diffconfig style; some packages may require additional feeds (e.g., luci-apps).

# PPPoE support (necessary for dial-up internet)
CONFIG_PACKAGE_ppp=y  # PPP protocol
CONFIG_PACKAGE_ppp-mod-pppoe=y  # PPPoE module
CONFIG_PACKAGE_luci-proto-ppp=y  # LuCI protocol for PPP/PPPoE configuration

# Core LuCI web interface (recommended for easy management)
CONFIG_PACKAGE_luci=y  # LuCI base
CONFIG_PACKAGE_luci-base=y  # Base LuCI modules
CONFIG_PACKAGE_luci-app-firewall=y  # Firewall app
CONFIG_PACKAGE_luci-app-opkg=y  # Package manager app

# Themes (recommended for better UI)
CONFIG_PACKAGE_luci-theme-bootstrap=y  # Default Bootstrap theme
CONFIG_PACKAGE_luci-theme-argon=y  # Argon theme (modern and customizable; if available in feeds)

# Networking and security plugins (recommended)
CONFIG_PACKAGE_luci-app-upnp=y  # UPnP for port forwarding
CONFIG_PACKAGE_luci-app-adblock=y  # Adblock for blocking ads
CONFIG_PACKAGE_luci-app-ddns=y  # Dynamic DNS
CONFIG_PACKAGE_luci-app-wireguard=y  # WireGuard VPN
CONFIG_PACKAGE_luci-app-unbound=y  # Unbound DNS resolver
CONFIG_PACKAGE_luci-app-sqm=y  # SQM for QoS/traffic shaping

# File sharing and access (recommended)
CONFIG_PACKAGE_luci-app-samba4=y  # Samba4 for file sharing
CONFIG_PACKAGE_luci-app-ttyd=y  # Web terminal

# Other utilities (recommended)
CONFIG_PACKAGE_luci-app-wol=y  # Wake-on-LAN
CONFIG_PACKAGE_openssh-sftp-server=y  # SFTP server
CONFIG_PACKAGE_ethtool=y  # Ethernet tool
CONFIG_PACKAGE_iptables=y  # IP tables
CONFIG_PACKAGE_ipset=y  # IP sets
CONFIG_PACKAGE_tc=y  # Traffic control
CONFIG_PACKAGE_zram-swap=y  # ZRAM for swap (useful for low RAM)

# Advanced/China-optimized (if available in ImmortalWrt feeds)
CONFIG_PACKAGE_luci-app-turboacc-mtk=y  # TurboACC for MTK acceleration
CONFIG_PACKAGE_luci-app-eqos-mtk=y  # EQoS for MTK
CONFIG_PACKAGE_luci-app-clash=y  # Clash proxy (if feed supports)
CONFIG_PACKAGE_luci-app-passwall=y  # Passwall (popular in ImmortalWrt for proxies; optional)

# Disabled unnecessary packages to save space
# CONFIG_PACKAGE_luci-app-ssr-plus=n  # Disable if not needed