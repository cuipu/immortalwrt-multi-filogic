# 设备配置：只针对 mediatek/filogic target 的 Redmi AX6000-ubootmod 变体
# 这个变体支持不死 U-Boot，推荐使用
# ---  核心目标硬件设置 (Target System) ---
CONFIG_TARGET_mediatek=y
CONFIG_TARGET_mediatek_filogic=y
CONFIG_TARGET_mediatek_filogic_DEVICE_xiaomi_redmi-router-ax6000-ubootmod=y

# ---  基础系统固件格式 ---
# 设置根文件系统分区大小（根据你的大分区uboot修改选择合适大小，通常128MB或更大）
CONFIG_TARGET_ROOTFS_PARTSIZE=128
# 生成 squashfs 格式的固件镜像，并进行 gzip 压缩
CONFIG_TARGET_IMAGES_GZIP=y
CONFIG_TARGET_KERNEL_PARTSIZE=16

# ---  必选无线驱动 (MT7986 专属) ---
# 包含 Wi-Fi 6 支持和固件驱动
CONFIG_PACKAGE_kmod-mt7986-firmware=y
CONFIG_PACKAGE_kmod-mt7915e=y
CONFIG_PACKAGE_kmod-mt798x-wmcu=y

# 启用一些默认功能（可选，根据需要添加）
CONFIG_PACKAGE_luci=y  # 启用 LuCI Web 界面
CONFIG_PACKAGE_luci-i18n-base-zh-cn=y  # 中文支持
CONFIG_PACKAGE_luci-ssl=y # 启用 HTTPS Web 界面
CONFIG_LUCI_SRCDIET=y

